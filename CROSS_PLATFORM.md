# 这里简要记录一下 Ren'Rs 的跨平台性！

目前 Ren'Rs 自动换了框架之后，不仅性能++，跨平台性也++了！

我们使用了 Webview（[Wry](https://github.com/tauri-apps/wry) + [Tao](https://github.com/tauri-apps/tao)） 的方式去绘制我们的 UI，性能接近原生！但是在不同操作系统上执行不同的操作。

适配了许多冷门的架构和操作系统！

只要各位的操作系统有**浏览器**这种东西，我就能保证各位的 vn 能够正常运行！

目前这个有两种编译方案：

1. 开通一个本地 HTTP 服务器，使用系统 Webview 编译出一个窗口，用于适配 steamworks。以及让玩家有更好的体验！
2. 开通一个本地 HTTP 服务器，以浏览器访问即可运行窗口界面！

但当各位只想编译 Windows、macOS 或者 x86_64 Linux 时，我强烈建议各位将 enabled-desktop 这个 features 给打开！因为这样的话，编译出来的就是一个窗口，不是浏览器了（

不过这个窗口目前仅限 Windows、macOS、Linux 平台！并且仅限 x86_64、arm64 架构！不能跨端编译！因为 Webview 只有这几个平台有（

上面我说的接近原生的意思，就是指当前的 Webview，由于 Web 的原因，运行效率肯定没有原生 UI 来的高，但我这边已经急切的把运行效率拉大了！

# 后端实现

说句实话，我们开发团队尝试过很多种不同的 UI 框架！下面介绍一下优缺点吧！

我将会从以下几点来介绍：

| Rust UI 框架 | Loongarch64、riscv64 上能否编译 | BSD 上能否编译 | Android、iOS 上能否编译 | 无 GPU 上能否编译 | 能否动态切换后端【OpenGL + llvmpipe】、【Vulkan + lavapipe】、【softbuffer + tiny-skia】 | 能否支持动态递归组件/动态 new 一个矩形到页面/组件树上 | 是否一套代码维护全端 |
|------------|---------------------------|-----------|-------------------|-------------|--------------------------------------------------------------------------|--------------------------------|------------|
| Tauri      | ❌                         | ❌         | ✅                 | ❌           | ❌                                                                        | ✅                              | ✅          |
| Dioxus     | ❌                         | ❌         | ✅                 | ⚠️          | ❌                                                                        | ✅                              | ✅          |
| Slint      | ✅                         | ✅         | ✅                 | ✅           | ✅                                                                        | ❌                              | ✅          |
| iced       | ✅                         | ✅         | ❌                 | ✅           | ✅                                                                        | ✅                              | ✅          |
| eframe     | ⚠️                         | ⚠️         | ✅                 | ✅           | ⚠️                                                                       | ✅                              | ✅          |
| Xilem      | ❌                         | ❌         | ❌                 | ❌           | ❌                                                                        | ✅                              | ✅          |
| Bevy       | ⚠️                        | ⚠️        | ✅                 | ✅           | ⚠️                                                                       | ✅                              | ✅          |

其中：

❌：代表完全不支持\
✅：代表完全支持\
⚠️：代表可能需要电脑做额外操作才能正常运行

上述为我们尝试过的 UI 框架！我们团队并没有采用上述的任何一个框架

不过我们团队现在正在使用（浏览器或者 Wry + Tao）作为首选框架！并且在将来都使用这个框架！

我们目前尝试的 Webview，将会在不同平台展现出不同的效果，因为 Webview 的局限性，如果硬要追求平台一致性，可能得要用 Electron 这种套 Chromium 的框架。所以为了减小二进制体积，我只能这么做。

各位可以随时随地在 features 里面启用 `enable-desktop` 去生成桌面程序！

如果各位想追求极致跨平台，这里也提供了一个非常棒的方式！那就是浏览器打开！只要各位的 PC 操作系统里面有浏览器，只要浏览器，就可以自动打开一个 HTTP 服务器端口！随后各位只需要在浏览器里面游玩即可！

不过，在跨平台方面我们是专业的！各位的 Android、iOS 默认启用的就是 wry 框架！也就是说，各位只能在 PC 端切换浏览器和内置 Webview 两种。。

在打包生成到 steam 时，我强烈建议各位将 `enable-desktop` 给打开！因为这样的话，就会生成的是桌面程序，而不是打开浏览器了（

在 `enable-desktop` 开启后，各位就可以很方便的执行某些需要权限才能执行的事情，比如说默认打开游戏就播放音乐等直接在前端里无法直接做到的事情。

并且，这里也同样提供直接部署前端的方法！在 features 里面将 enable-webbrowser 给打开即可！这种办法将生成一个纯前端应用！将会在你的 /dist 目录里直接生成一个前端应用！我们甚至可以直接部署到 github.io 甚至哪都可以（只要你的域名支持即可）！
